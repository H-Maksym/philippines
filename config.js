const tailwind = {
  tailwindjs: './tailwind.config.js',
};

// tailwind plugins
const plugins = {
  typography: true,
  forms: true,
  containerQueries: true,
};

export default {
  tailwind,
  plugins,
};
